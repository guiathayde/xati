import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#FFF',
    borderRadius: 10,
    borderWidth: 1,
    borderColor: '#aab0b7',
    marginBottom: 4,
    marginLeft: 10,
    marginRight: 10,
    alignSelf: 'center',
    justifyContent: 'center',
  },
});
